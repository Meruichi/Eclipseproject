function animal_sendit(){
	// validationcheck 구분 성별 크기 연령 이름
	let frm = document.animalForm;
	let animal_species = frm.animal_species; 			
	let animal_gender = frm.animal_gender;		
	let animal_weight = frm.animal_weight;		
	let animal_age = frm.animal_age;		
	let animal_name = frm.animal_name;		

	if(animal_species.value == ""){ // 구분 관계없이 모두선택(All)
		animal_type.
		return ;
	}
	if(animal_gender.value == ""){ // 구분 관계없이 모두선택(All)
		animal_type.
		return ;
	}
	if(animal_weight.value == ""){ // 구분 관계없이 모두선택(All)
		animal_type.
		return ;
	}
	if(animal_age.value == ""){ // 구분 관계없이 모두선택(All)
		animal_type.
		return ;
	}
	if(animal_name.value == ""){ // 구분 관계없이 모두선택(All)
		animal_type.
		return ;
	}
	frm.submit();
}